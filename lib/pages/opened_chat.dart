import 'package:flutter/material.dart';




class OpenedChatPage extends StatelessWidget {
  const OpenedChatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child:Text("Opened chat page"),);
  }
}